{
  "name": "artist-network",
  "version": "2.1.0",
  "description": "Artist influence network — cosmos.gl",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@cosmos.gl/graph": "^2.6.4"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "gh-pages": "^6.1.1"
  }
}
